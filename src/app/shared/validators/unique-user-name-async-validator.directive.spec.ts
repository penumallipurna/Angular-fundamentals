import { UniqueUserNameAsyncValidatorDirective } from './unique-user-name-async-validator.directive';

describe('UniqueUserNameAsyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueUserNameAsyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
